@{var width = 100 - Convert.ToInt16(ViewBag.Percent);}
@if (ViewBag.Percent != "100")
{
    <div title="Click" class="progress progress-striped">
         <div class="bar" style="width: @ViewBag.Percent%;"></div>
    </div>
    @ViewBag.Percent <text>% Completed</text>
    
    <br />
}